# 大地图战斗系统全面改造方案

版本：v1.1。建立日期：2026-09-09，实施状态更新：2026-09-11（Asia/Shanghai）。
状态：G0、G1、G2 已完成；G3 至 G5 待实施。当前本地七塔八级，仅箭塔/毒蚀塔/指挥塔完成技能与专精。
适用工程：`tower-defense`，原生 JavaScript ES Modules + Three.js。

## 0. 接手须知

用户最初要求整理目标方案，随后已明确授权按文档持续实施。当前按里程碑推进，下一阶段为 G3；没有自动发布授权。

- 用户明确要求：适配扩大后的地图，全面升级攻击算法、金币掉落、塔等级、怪物等级、塔技能，并至少再增加五六种塔。
- 内容最低目标：保留原有 5 种，新增 6 种有独立战术用途的塔，总计至少 11 种。不能仅换名字、模型或颜色。
- 本文的 8 级塔、6 种新塔方案、技能分支、怪物等级范围、解锁时机和数值都是建议基线，不是用户逐项指定的参数。实施中可据实测调整，但必须记录原因，不能缩水必做范围。
- 接手从第 11 节第一个未完成阶段继续，目前为 G3。不要重复已验证的 G0 至 G2，不要只加图鉴数据就宣称完成。
- 保留已被用户认可的大地图、独立路线、分岔汇流、地形氛围、小地图和镜头操作。当前工作区有未提交改动，不能用旧提交覆盖它们。
- 当前本地版本不等于线上版本。本文及历史部署记录均不构成发布授权；默认本地开发、测试和交接，不自动推送、部署或提交平台审核。
- 本文描述的新增模块、命令和报告会明确标为“拟新增”，不得当作已经存在。历史材料见 [TASK_STATE.md](TASK_STATE.md)，运行入口见 [README.md](README.md)。

## 1. 目标与边界

| 编号 | 必做目标 | 完成的含义 |
|---|---|---|
| R1 | 大地图攻击算法升级 | 多路威胁判断、可靠射程校验、转弯预判、伤害结算、范围查询和性能均适配 |
| R2 | 金币与经济重做 | 开局、击杀、清波、提前开战、升级和出售形成可审计的收支闭环 |
| R3 | 塔成长升级 | 不只是加几行数值；提供更长成长线、阶段能力和明确的升级取舍 |
| R4 | 怪物等级升级 | 等级、普通/精英/Boss、抗性和波次压力有统一规则，不能只显示等级标签 |
| R5 | 塔技能体系 | 旧塔和新塔均接入，支持冷却、触发、状态、分支、反馈与测试 |
| R6 | 新增至少 6 种塔 | 合计至少 11 种，模型、图标、建造、升级、技能、售卖、模拟器全部可用 |
| R7 | 界面与存档兼容 | 十一种塔在桌面/移动端都能操作，不遮挡战场，不丢失已有星级和设置 |
| R8 | 可验证交付 | 地图回归不退化；新增战斗测试、50 关经济报告、多策略验证和性能记录 |

设计目标：扩大的是防线经营和战术选择，不是单纯等待时间。前期有钱建立合理防线，中期需要应对兵种和路线，后期有高阶投资目标且不能一套塔通吃所有情况。

本轮不包含：再次扩地图、重做引擎、联网多人、付费系统、随机装备、离线收益、永久属性养成、塔受损维修、阻挡道路/动态寻路或完整中途对局存档。不得顺手引入这些系统扩大范围。

## 2. 经代码核对的当前基线

以下是规划时实际代码状态，不是目标状态。后续应重新核对，不能只信旧注释。

| 项目 | 当前事实 | 主要位置 |
|---|---|---|
| 战场 | 42×28 格，单格 1 单位；原 22×15，面积约 3.56 倍 | [config.js](js/game/config.js) |
| 关卡 | 5 世界、50 关独立路线，其中 20 关为双路；河流、桥梁、地标与建造限制一致 | [maps.js](js/game/maps.js)、[map-layout.js](js/game/map-layout.js) |
| 塔 | arrow / cannon / frost / tesla / sniper，均为玩家可见 Lv.1 至 Lv.5；内部索引 0 至 4 | [towers.js](js/game/towers.js) |
| 索敌 | 遍历敌人，按所属路线的剩余距离选目标，约每 0.15 秒重选；炮弹用当前切线估算落点 | [entities.js](js/game/entities.js) |
| 伤害与效果 | 固定值护甲扣减、忽略护甲、护盾、单槽减速；炮击、冰环和闪电链是特定分支 | [entities.js](js/game/entities.js)、[battle.js](js/game/battle.js) |
| 怪物 | 11 种普通敌人定义、5 种 Boss；没有统一的独立等级和精英词缀体系 | [units.js](js/game/units.js) |
| 强度 | 关卡 HP/速度/赏金倍率与波次爬坡共同作用；实际每关 6 至 16 波 | [levelgen.js](js/game/levelgen.js) |
| 经济 | 开局金 280 至 1082，击杀赏金、清波奖金、提前开战奖励；出售按实际投入的 70% | [battle.js](js/game/battle.js)、[entities.js](js/game/entities.js) |
| 界面 | 塔坞和快捷键写死五塔，满级文字写死 Lv.5；已有小地图、暂停和移动端手势 | [hud-lite.js](js/ui/hud-lite.js)、[main.js](js/main.js) |
| 存档 | `td_save_v1` 存星级、设置、教学和管理员标记；不保存战斗内塔或怪物 | [save.js](js/core/save.js) |
| 测试 | `npm.cmd test` 当前运行地图及多路战斗的 57 项测试，2026-09-09 本轮重跑全部通过 | [maps.test.mjs](tools/maps.test.mjs)、[package.json](package.json) |
| 测试 | `npm.cmd test` 当前运行地图及多路战斗的 67 项测试，2026-09-09 本轮全部通过 | [maps.test.mjs](tools/maps.test.mjs)、[combat.test.mjs](tools/combat.test.mjs)、[package.json](package.json) |

上一轮地图浏览器检查覆盖五主题及 1440×900、720×900、390×844、844×390 四种视口，通过截图生成、像素、几何、建造、导航和双路行军断言。该结果不证明本方案的新系统已通过测试；截图生成也不等于人工目测。

### 必须先处理的扩展阻碍

1. `Battle.costOf()`、HUD 的 `DOCK`、`main.js` 的 `AUTO_COSTS` 和多个探针的 `COSTS` 各维护一份造价。必须统一读取塔定义，未知 key 必须拒绝，不能默认 0 金。
2. `Tower.fire()` 的多种攻击及 `Battle.explode()`、治疗都直接扫敌人数组。加入持续区域、光环和更多塔后，需要共用空间查询。
3. `pierce` 现在表示忽略护甲，不表示穿过多个敌人。新重弩不能复用同一个布尔值表达穿透数量。
4. 索敌、开火、命中应分别复核目标资格；新增 `air` 目标类型后，脉冲、连锁和爆炸也必须执行一致的空地过滤。
5. `Enemy.reward` 当前最低为 1。将来零赏金召唤物不能继续被这一保底强制变成 1 金。
6. 护盾完全吸收和护盾被打破时的伤害返回口径不统一；新统计、吸血或击杀归因不能直接依赖旧返回值。
7. `sellSelected()` 目前只移除塔模型；等级刻度也会重建。更多塔和等级下要核查私有资源释放，保留共享模型/几何缓存。
8. 旧注释、历史 TASK_STATE、模拟器策略仍有 30/40 关、小地图尺寸和五塔假设。不能把旧机器人胜率、固定八塔阵容当成新版平衡目标。

## 3. 攻击算法与战斗基础

### 3.1 多路线索敌

- 为每个敌人提供 `remainingDistance`、`effectiveSpeed`、`estimatedExitTime`、`routeId` 和稳定 ID。瞬时停滞的预计到达时间使用有限的安全处理，不产生 NaN。
- 建议默认“最接近突破”：优先预计到达出口时间较短者，而不是只比较已走距离、归一化进度或地图直线距离。相同速度时保留按各自路线剩余距离的顺序。
- 支持“最前、最后、最强、最弱、最快、支援优先”策略。最强/最弱使用明确定义的当前有效耐久，含护盾；支援优先找治疗或召唤单位，找不到再回退默认策略。
- 路线在空间中靠近或汇合时，不得混用两个 sampler。评分相同时用稳定 ID 破同分，避免目标每帧乱跳。
- 取得目标后，在开火瞬间检查存活、HP、空地类型、射程、技能限制和瞄准。越界、死亡或漏出的对象不能靠索敌缓存继续被新攻击。
- 射程和范围效果统一使用 XZ 战场平面距离，视觉高度单独用于弹道、动画与命中特效。当前地形不增加视线遮挡规则。

### 3.2 空间查询与攻击浪费

- 使用适配固定地图的均匀网格索引，建议初始桶宽 4 格；按查询半径枚举所有相交桶后精确过滤，长射程不能只查固定九格。
- 移动、出生、死亡、召唤和离场都应同步索引。治疗、溅射、连锁、毒区和光环复用相同查询接口。
- 建议有效目标存在时每 0.10 秒重选，按塔 ID 分散查询时刻；非法目标立刻触发重选。这个间隔是调优起点，不是正确性的替代品。
- 对高伤慢射塔建立保守的在途伤害预估，减少多塔对残血敌人的过量集火。不得把预测伤害直接扣血，或因为“有人在打”而无限忽略紧急目标。
- 炮弹可能落空、持续伤害可能未走完，不能按必定命中来预留全部伤害。死亡、命中、目标失效或弹体超时必须释放预留。

### 3.3 弹道与命中

- 炮击按 `sampler.at(dist + effectiveSpeed * flightTime)` 预测路线位置，可做有限次飞行时间修正；不能沿转弯处切线直接射出地图。预测不保证追踪目标的未来减速和加速。
- 引导弹、炮弹、穿透直线、扇形、连锁、范围脉冲分开描述几何规则，保留现有已经正确工作的动画和瞄准机制。
- 穿透弹使用扫掠线段检测，避免高速弹穿过敌人却漏判；同一弹体对同一敌人只能命中一次，受穿透数量和最大飞行距离限制。
- 所有弹体有寿命、越界和非有限值回收。目标死亡后应按攻击类型消失或飞向最后落点，不能任意瞬间转移伤害。
- 伤害和效果的攻击方数值在发射时快照，目标抗性在命中时读取。升级、出售或光环变化不能追溯修改已发出的弹体。

### 3.4 统一伤害协议

建议先建立下列概念契约，字段名可按现有代码风格调整，不需要迁移到 TypeScript：

```text
AttackSpec: sourceTowerId, sourceKey, attackId, damageType, baseDamage,
            targetMask, ignoreArmor, armorPenetration, pierceCount, effects
DamageResult: hpDamage, shieldDamage, overkill, killed
Query: queryEnemiesInRadius / queryEnemiesAlongSegment / queryTowersInRadius
```

- 伤害分为物理、法术、真实。真实伤害默认绕过护甲和抗性，但不绕过护盾；个别破盾技能必须显式配置。
- 固定结算次序：攻击方快照及技能倍率 → 护甲/抗性与穿甲 → 护盾 → HP → 死亡事件。伤害统计只记实际损失，过量伤害单独记录。
- 护甲建议改为收益递减模型，例如 `K / (K + armor)`，`K=100` 仅为初始候选。现有 0 至 18 的固定减伤值必须重标定，不可原样套入新公式。抗性、减伤、穿甲有明确上下限。
- 不把每次 DoT 或零伤害事件强制抬到 1 点；内部允许小数，显示时再格式化。负伤害、无效来源和非法配置必须有可测的拒绝行为。
- 同一敌人的死亡、赏金、分裂、战绩和漏怪结算必须幂等。约定同一步已判定死亡的对象不再扣生命、不被治疗、不重新索敌。

### 3.5 统一时间语义

- 逻辑推进使用同一模拟时钟，建议 1/60 秒固定步长；1x/2x/3x 通过累计模拟时间推进，不各自乘在技能、弹体和状态内部。
- 同种子、同一模拟时刻的操作序列，在不同渲染帧率和倍速下应得到一致结果。纯外观随机数不能改变掉落、技能或出怪。
- 暂停时攻击、冷却、状态、出怪和波间奖励倒计时冻结；页面长时间后台后不积累巨量补帧瞬间判负。
- 固定步长落地时同时调整模拟器，不能让模拟脚本再乘一次速度；为一次长 dt 与多次短 dt 写等价性测试。

## 4. 金币掉落与经济闭环

### 4.1 基本原则

地图面积扩大不代表金币、射程、怪物数量或血量都乘 3.56。平衡要看有效覆盖时间、同时守住的路线、单位密度和玩家可部署火力。

- 保留战斗内金币，不新增永久货币和利息。金币自动结算，视觉掉落只做反馈，不能要求在大地图逐个点击拾取。
- 开局先保证有合法的防守方案，而不是按总可建塔格发钱。建议第一关以 400 金试配；双路是否额外补金，由入口覆盖成本决定，不无条件翻倍。
- 原五塔购买价可先保留作为比较锚点，但本轮允许整体重标定；不能因为历史文档写“世界 1-2 不动”就拒绝用户本次的系统改造。
- 必须保留前期可理解、可起步的手感。不得让玩家必须卖塔套利或提前开战才买得起首波必要克制单位。

### 4.2 收入组成

| 来源 | 目标规则 |
|---|---|
| 开局金 | 按世界/关卡预算与多路起防成本定标，同关重试一致 |
| 普通击杀 | 根据兵种、等级和波次奖励预算确定，不跟随 HP 后期倍率无限增长 |
| 精英/Boss | 有限的高价值目标奖励，计入波次预算，不能击杀后再重复发基础赏金 |
| 清波奖 | 战斗阶段的稳定收入；包括最终波，进入结算界面前只发一次 |
| 提前开战 | 保留风险换金币；只能领取一次，建议不超过该波常规收入的 15% |
| 召唤/分裂 | 生成时明确奖励归属、总预算和代数上限，不能成为无限刷金来源 |
| 出售 | 先沿用实际净投入的 70%，统一取整，普通升级和专精升级都只记一次 |

建议召唤类初版规则：周期召唤物奖励为 0；死亡分裂可在父代生成时预留有限的子代赏金份额。全家族的可得赏金不能超过该组预算，子代继承路线、等级快照和家族 ID；禁止无上限递归分裂。

### 4.3 支出与成长节奏

- 支出只来自建塔与升级。Lv.6 的专精选择包含在该次升级报价中，不另收第二笔费用。初版不增加技能施放金币消耗、随机掉落或可循环赚钱的经济塔。
- 升级价由同一份定义提供，遵循逐阶增长；高阶费用对应新能力，不再只靠突然大幅涨价吸收金币。
- 同预算下，新增一座低级塔与升级一座主力塔都应在某些地形/敌情下有意义；支援塔收益必须包含机会成本。
- 目标是常规打法每 1 至 2 波至少有一次有意义的建造或升级选择。后期既不能长时间无事可做，也不能长期全塔满级且金币没有用途。
- 不以“剩余金币越少越好”定平衡。保留玩家有意储蓄、跨路补防和为高阶技能攒钱的空间。

### 4.4 经济账本

每个关卡与波次记录开局金、击杀/清波/提前开战/出售收入、建塔/升级支出（Lv.6 标注所选专精）、期末金币、塔总投资和未消耗预算。强制满足：

```text
期末金币 = 期初金币 + 所有已结算收入 - 所有实际支出
家族赏金总额 <= 该敌人组预分配的奖励预算
出售返还 <= 该塔实际投入；建造升级出售循环不能净增金币
```

关键事件附稳定 ID。失败操作、重复点击、重复命中、最后一波及退出重开都纳入测试，不能靠界面按钮暂时不可见来保证账本正确。

## 5. 塔等级与分支成长

建议所有塔从 5 级扩展为 8 级；内部继续使用 0 基索引，UI 显示 1 至 8，最大等级从定义读取。未改变玩家塔和格子的基本占地。
本方案的初始验收按 8 级设计；若根据证据修改成长上限，必须同步更新内容表、里程碑和验收参数，不能只改代码。

| 玩家等级 | 目标 |
|---|---|
| Lv.1 | 具备兵种定位的基础攻击或支援 |
| Lv.2 | 可负担的第一轮强化，改善开局稳定性 |
| Lv.3 | 成型基础属性，不能把其他塔角色全部替代 |
| Lv.4 | 解锁招牌自动技能，配套首次明显的模型变化 |
| Lv.5 | 强化技能效率与持续输出 |
| Lv.6 | 从 A/B 两条专精中选一条，单次升级扣费；本局不可反复切换刷收益 |
| Lv.7 | 强化已选专精 |
| Lv.8 | 解锁终阶技能和最终外观，默认自动，可切换手动施放 |

- 不增加另一套杀敌经验升级；先以金币升级完成清晰、可审计的成长线，避免支援塔因没有击杀而无法成长。
- 初版专精只影响本局这座塔，不做永久天赋树。重新选择需正常出售重建，不能重置冷却后获利。
- 属性由基础值、等级覆盖、专精和临时效果推导，不直接反复修改基础定义。HUD、预览、模拟器读取同一份有效属性。
- 攻速、射程、减速、增益和冷却缩减需要上限。射程保持职业差异，不能全塔升级到覆盖整张地图。
- 建议 Lv.1/Lv.4/Lv.8 至少三档可区分外观，Lv.6 的专精有小型标记；不能只增加越来越多的等级小方块。

## 6. 十一种塔的内容方案

### 6.1 六种新塔

以下 key 是建议稳定标识。初始造价是调参起点；不会在本规划轮写入代码。

| 新塔 / key | 核心机制与目标 | 擅长 / 短板 | 试配造价 | 建议最早可用关卡 |
|---|---|---|---|---|
| 烈焰塔 / `flame` | 近程扇形法术伤害与燃烧，仅对地 | 清密集地面兵；怕飞行和远处多路分散 | 140 | W1-3 |
| 毒蚀塔 / `venom` | 毒弹、有限叠层 DoT、治疗抑制，对地 | 长路线消耗与克制治疗；爆发弱、怕快速漏怪 | 120 | W1-7 |
| 重弩塔 / `ballista` | 沿直线穿透多个地面敌人，物理破甲 | 长直路成列目标；弯道和散兵收益低 | 180 | W1-9 |
| 防空塔 / `flak` | 仅锁定飞行单位，快速弹或小范围空爆 | 飞行编队和重型飞行；不能顺便清地面兵 | 125 | W1-5 |
| 奥术塔 / `arcane` | 法术单体、额外护盾损伤、短暂抑制护盾再生，对空对地 | 护盾、护甲型精英；对高法抗与廉价人海效率低 | 210 | W2-3 |
| 指挥塔 / `beacon` | 区域友军增益与战术技能，无直接伤害 | 高投入主力塔集群；铺得分散或支援过多会损失输出 | 200 | W2-1 |

解锁以关卡进度定义，进入相应关卡时可用，不额外收费。原五种塔保持可用；旧存档自动按已有进度得到应有的新塔。防空塔必须早于首次强制飞行压力解锁，毒蚀塔必须早于以治疗兵为主题的高压力波次解锁。若调整出怪顺序，同步修改解锁表。

### 6.2 全部塔的技能与专精草案

| 塔 | Lv.4 招牌技能 | Lv.6 A / B 专精 | Lv.8 终阶技能 |
|---|---|---|---|
| 箭塔 `arrow` | 周期连射，同目标或有限分配 | 快速清杂 / 单点压制 | 局部箭雨，受对空对地规则约束 |
| 炮塔 `cannon` | 破片溅射 | 大范围集群压制 / 小范围重击破甲 | 多轮定点炮击，仍仅对地 |
| 寒霜塔 `frost` | 冰寒累积，有限触发短冻结 | 深寒控制 / 广域减速 | 暴风雪，不得无限冻住 Boss |
| 特斯拉塔 `tesla` | 周期过载链 | 更多跳跃 / 更强首击及较低衰减 | 局部雷暴，同一条链不重复跳同敌 |
| 狙击塔 `sniper` | 优先标记高威胁目标 | 极重单发 / 快速转换目标 | 蓄力狙杀，非无条件斩杀 Boss |
| 烈焰塔 `flame` | 燃烧与短时残火 | 更宽扇面 / 更强持续灼烧 | 范围火场，有明确消失时间 |
| 毒蚀塔 `venom` | 中毒附带治疗抑制 | 有限传播 / 单体叠层 | 毒云区，叠层与传播数量受限 |
| 重弩塔 `ballista` | 穿透命中附破甲 | 更多穿透 / 重型目标首击强化 | 蓄力贯穿，仍需有效线段命中 |
| 防空塔 `flak` | 编队拦截爆发 | 对快速小型飞行 / 对重型飞行 | 空中齐射，不伤地面目标 |
| 奥术塔 `arcane` | 破盾与再生抑制 | 护盾克制 / 法术单体输出 | 能量裂解，伤害及破盾分别计账 |
| 指挥塔 `beacon` | 局部主力火力增益 | 攻击增益 / 技能循环支援 | 短时集结强化，不影响其他指挥塔 |

原有五塔的角色必须保留：箭塔通用、炮塔清地面群体、寒霜控场、特斯拉连锁、狙击远射穿甲。新塔应创造额外阵容，而不是把原塔全部变成过渡品。

新塔完成清单：定义、购买校验、攻击、八级属性、两条专精、技能、目标策略、模型、图标、音效、升级/出售、解锁、HUD、模拟策略和测试。任一项缺失，不能在交接时标记整座塔完成。

## 7. 怪物等级、精英与波次

### 7.1 等级与属性

- 建立 `enemyLevel`、`rank`（普通/精英/Boss）、兵种基础模板与成长曲线；建议等级覆盖 Lv.1 至 Lv.100，以关卡基础等级和波次偏移生成。
- 等级实际影响 HP、护甲/法抗、护盾或兵种技能强度；速度只做有上限的小幅变化，不能随等级无限加速。
- 等级、rank、词缀与波次压力只应用一次。旧 `hpMul` 和 `waveHpMul` 迁移时必须明确替代关系，不能在新公式后再隐式乘一遍。
- 避免“血条越来越长但玩法不变”：编队、密度、治疗、护盾和路线分配共同制造压力。保留已有模型、动画与朝向修复。
- 界面区分等级、精英标记、Boss 标记和抗性，不只靠模型放大或颜色变化。

### 7.2 精英与克制

初版建议五种可配置精英词缀：重甲、护盾、疾行、再生、坚韧。普通精英最多一个主要词缀，后期允许两个兼容词缀；不能生成无解组合或永久控制免疫。

Boss 保留主题技能并补齐成长和控制抗性。冻结/减速对 Boss 使用明确的折减与恢复窗口，而非忽然完全失效；奥术破盾、毒蚀抑疗等功能仍需产生可观察作用。

### 7.3 波次预算

- 每波先确定威胁预算、经济预算、兵种主题、等级区间、线路分配与出怪持续时间，再生成敌人组。精英和 Boss 消耗预算，不是在生成完普通波后无成本叠加。
- 威胁估算至少考虑有效耐久、速度、空地类型、治疗/召唤能力和多路覆盖成本；威胁值与奖励值相关但不等同。
- 保留固定种子和同关重试布局；词缀、路线分配和奖励不随刷新随机变化。波次预览使用生成后的真实数据。
- 双路可有主次压力和轮换，但开波前必须能看出入口与主要威胁；不能在没有提示、没有可用克制塔时突然推出必漏阵容。
- 初版继续沿用“清波后进入下一波/休整”的流程，不同步引入无限重叠波次；长路线节奏优先通过覆盖、波数组成和出怪间隔调整。

## 8. 技能与状态的统一规则

每个技能定义触发方式、目标规则、冷却、持续时间、效果、自动施放条件和视听反馈。机制先统一，再填写十一种塔的内容。

| 系统 | 必须明确的规则 |
|---|---|
| 持续伤害 | 同来源刷新或叠层、跨来源叠加上限、固定 tick、攻击方快照；禁止逐帧扣一次完整伤害 |
| 减速 | 建议同类取最强，初始普通目标总减速上限 75%；较弱长减速不能无限续上较强短减速 |
| 冻结/控制 | Boss 有折减、连续控制递减及解控窗口；初值用测试和实机调优 |
| 治疗抑制 | 按实际治疗入口生效，不让生命变负、不把受伤变成治疗；同类效果取最强 |
| 破甲/破盾 | 护甲下降、穿甲比例、额外护盾伤害分清，不复用一个 `pierce` 开关 |
| 光环 | 同组取最强，有覆盖范围和失效处理；支援塔不互相增幅、不增幅自己、不生成金币 |
| 技能冷却 | 使用模拟时钟；初始、升级、专精、自动/手动切换均不得刷掉已有冷却 |
| 出售与在途效果 | 出售立刻终止光环和持续引导；已发出的弹体/有时限地面区按快照完成，来源失效仍可安全计账 |

Lv.8 技能默认自动，保证只用自动技能也有合法通关方案；熟练玩家可以手动优化时机。初版手动施放限选中塔及其有效作用范围，不增加全图无条件攻击。

有目标技能在取消、无合法目标、暂停或目标点越界时不消耗冷却。新建/重新建造的高阶技能不能绕过初始冷却；升级按剩余冷却比例迁移并设置边界。所有取整、叠加、持续时间和控制上限集中配置。

## 9. 界面、资产与存档

### 9.1 十一种塔的操作

- 塔坞从注册表生成，不继续手写造价和名称。建议按 5/5/1 分三页，第一页保留旧五塔顺序，必要时增加类别筛选。不能把十一张卡硬塞在一排。
- 数字键 1 至 5 对应当前页可见槽位，分页使用单独图标按钮；Q/E 继续旋转地图，不能挪作翻页。未解锁塔显示解锁条件，不允许购买接口绕过。
- 选中塔面板显示等级上限、有效属性、升级前后差异、专精、目标策略、技能冷却、自动/手动开关与实际出售价格。
- 导航/缩放/施放使用图标按钮和提示，策略用选项菜单，自动施放用开关，内容分组用页签。移动端按钮可触达，不能与小地图、建塔栏、取消操作重叠。
- 输入区分导航、建造、选塔与技能选点。取消技能不会顺手建塔；点击 HUD 不会穿透地面；聚焦按钮时 Enter/Space 不误触开波。
- 下一波展示实际兵种、等级区间、精英词缀、Boss 和入口分配。敌人详情与战后统计能说明威胁和克制，不堆放大段功能介绍。

### 9.2 新资产与回退

- 六座新塔各有可辨识的 3D 造型、塔坞图标、弹道/技能反馈和音效，不接受六个换色占位物作为最终交付。
- 优先复用项目已有 Three.js、Kenney 模型和资源管线，新增资产记录来源与许可。程序化回退可用于开发及加载失败，但也要保留塔的辨识度。
- 补齐 `assets/textures/ui/tower_<key>.png`，或统一调整为注册表里的真实图标路径；不能只生成资源却没有接入打包。
- 技能特效限池、限数量、限寿命；视觉密度不能遮住路、敌人和技能选点。高等级外观不扩大建造格占地。

### 9.3 兼容现有存档

- 保留已有星级、关卡解锁、设置、教学和管理员开关。新增塔解锁优先由星级进度推导，不发永久属性加成。
- 只有新增持久字段确有需要时才升级存档 schema；迁移必须读取现有 `td_save_v1`，不能只换 key 造成进度丢失。
- 对旧字段缺失、损坏 JSON、迁移重复执行分别测试；现存合法星级和音量/画质设置不因迁移丢失。
- 当前未保存战斗内塔，无需伪造“旧塔等级存档迁移”。如以后新增局内存档，另立需求，不隐式加入本轮。

## 10. 代码落点与边界

保持当前架构，逐步抽取必要逻辑。不要为了设计文档一次新建大量空模块，不引入新前端框架、通用 ECS、后端或第二套游戏实现。

| 已有位置 | 改造职责 |
|---|---|
| [towers.js](js/game/towers.js) | 十一种塔定义、等级/专精数据、共享造价和模型工厂入口 |
| [units.js](js/game/units.js) | 怪物模板、Boss、可用词缀与技能元数据 |
| [entities.js](js/game/entities.js) | 实例状态、索敌和弹道接入、动画适配、资源生命周期 |
| [battle.js](js/game/battle.js) | 统一逻辑推进、出怪/死亡、金币账本、建造事务与命令校验 |
| [levelgen.js](js/game/levelgen.js) | 等级曲线、关卡/波次预算、经济参数和解锁顺序 |
| [hud-lite.js](js/ui/hud-lite.js)、[main.js](js/main.js) | 注册表塔坞、面板、技能输入、固定时间驱动、自动玩家 |
| [minimap.js](js/ui/minimap.js)、[style.css](css/style.css) | 十一种塔的识别与新面板避让，保留现有导航行为 |
| [save.js](js/core/save.js)、[result.js](js/ui/result.js) | 必要的兼容迁移、解锁和战后统计 |
| [audio.js](js/core/audio.js)、[modellib.js](js/engine/modellib.js) | 新技能反馈和资源接入；不得破坏模型归一化/朝向/动画绑定修复 |
| [sim.mjs](tools/sim.mjs)、[balance-probe.mjs](tools/balance-probe.mjs)、[econprobe.mjs](tools/econprobe.mjs) | 注册表造价、技能/分支决策、多路布防、多策略与账本统计 |
| [build-dist.mjs](tools/build-dist.mjs)、[make-art.mjs](tools/make-art.mjs) | 核对新增图标/模型产物是否纳入生成和本地构建，不执行发布 |
| [maps.test.mjs](tools/maps.test.mjs)、[map-browser-check.mjs](tools/map-browser-check.mjs)、[package.json](package.json) | 保留地图回归，将新增测试纳入默认命令 |

拟新增的最小职责模块，可按实施需要合并，当前尚不存在：

- `js/game/spatial-index.js`：敌人/塔的局部查询，不依赖 DOM。
- `js/game/combat.js`：目标资格、伤害类型和结算结果；不负责渲染。
- `js/game/effects.js`：状态叠加、持续伤害、控制与光环；不直接修改全局模板。
- `js/game/skills.js`：技能定义、触发和冷却；手动/自动/机器人使用同一入口。
- `js/game/economy.js`：预算与账本校验，必要时抽取；保留 `Battle` 的编排责任。

不要求预先拆出更多文件。等级与解锁数据先归到现有定义/关卡模块，只有确有复杂度时再拆。尽量使用稳定 key 和清晰契约，避免“很多类但战斗仍然依赖五塔硬编码”。

## 11. 分阶段实施与交付

G0 基线、G1 战斗基础与 G2 成长技能闭环已完成。当前七塔八级，箭塔/毒蚀塔/指挥塔支持招牌、专精与终阶；地图与地形保持。G3 怪物及经济、G4 其余四座新塔与完整十一塔技能、G5 全量定标尚未完成。验证和已知限制见 [G2 验证报告](docs/G2_VERIFICATION.md)。

| 阶段 | 工作范围 | 必须留下的结果与通过条件 |
|---|---|---|
| G0 基线与约束 | 核对工作区、记录旧五塔、50 关收入/漏怪/耗时、盘点硬编码，固定测试种子与参考设备 | 可复现的基线记录；地图测试通过；明确旧模拟器的局限；不先调数值 |
| G1 战斗与数据基础 | 注册表单一数据源、时间推进、空间索引、统一目标/伤害/死亡协议、弹道修正 | 旧五塔仍可玩；未知塔不能零元生成；空地/多路/伤害/倍速/幂等测试通过 |
| G2 成长和技能闭环 | 八级/专精/技能通用能力；先完成旧箭塔及新毒蚀塔、指挥塔；最小可操作面板 | 能真实建造、升级、选分支、自动/手动施放和出售；DoT、光环、冷却与费用测试通过 |
| G3 怪物和经济重构 | 怪物等级/rank/词缀，统一波次与经济预算，召唤家族、账本、模拟器统计 | 生成 50 关数据无 NaN/无未定义兵种；等级与倍率不重复；收支及奖励测试通过；暂不宣称最终平衡完成 |
| G4 完整内容和体验 | 其余四座新塔、剩余旧塔八级技能、十一塔 UI、解锁、资源、存档兼容 | 十一种塔均达到第 6 节完成清单；全套克制关系可用；四视口真实交互和资源检查通过 |
| G5 全量定标与验收 | 50 关多策略、金币曲线、Boss、性能/内存、截图和输入回归，修整参数 | R1 至 R8 逐项有证据；本地可体验；附已知限制和验证报告；不自动发布 |

阶段依赖为 `G0 → G1 → G2 → G3 → G4 → G5`。G3 中尚未完成的新塔可用离线数值原型辅助校准，但不能把依赖未实现克制塔的关卡当成可玩成品。G4 后必须重做全量定标。

可执行清单：

- [x] G0：基线和约束已记录
- [x] G1：旧五塔在新战斗基础上回归通过
- [x] G2：成长/技能与两座新增塔闭环通过
- [ ] G3：新怪物等级、波次和金币闭环通过
- [ ] G4：六座新塔、十一塔完整内容与 UI 通过
- [ ] G5：50 关平衡、性能和兼容验收通过

每阶段要有可独立体验的版本、实际执行的测试和剩余事项。技术基础改造与最终数值调参分开记录，便于定位回归；不要为拆阶段回滚用户已有地图改动。

## 12. 测试、平衡和完成标准

### 12.1 逻辑测试矩阵

拟新增 `tools/combat.test.mjs`、`tools/economy.test.mjs`、`tools/progression.test.mjs` 和 `tools/skills.test.mjs`，按职责合并也可。新增后必须修改 `package.json` 的测试入口；目前 `npm test` 只运行 `tools/maps.test.mjs`。

| 测试域 | 最少覆盖案例 |
|---|---|
| 注册表 | 十一种塔可读；UI/造价/升级一致；未知 key、未解锁、越级与金币不足被拒绝 |
| 索敌 | 不同长度/不同速度的双路，出口紧急目标、平局稳定性、失效目标、开火越界 |
| 目标类型 | 对地不伤飞行、对空不伤地面；溅射/穿透/连锁/技能不能绕过过滤 |
| 弹道 | 弯道预判仍在所属路线、高速穿透不漏判/不重复、目标死亡/漏出和超时回收 |
| 伤害 | 物理/法术/真实、护盾全吸收及击穿、0 点/小数伤害、减伤上限、实际伤害与过量伤害 |
| 死亡 | 同一步多塔/DoT 命中只死亡掉金一次；不重复召唤，不再治疗，不死亡后漏怪 |
| 金币 | 普通/精英/Boss/最终波/提前开战、召唤 0 金、家族上限、重复点击、失败操作与出售不套利 |
| 成长 | 全部十一塔的 Lv.1 至 Lv.8、两分支费用、满级边界、旧攻击方式与范围不丢字段 |
| 状态和光环 | 强弱减速交叠、毒层上限、抑疗、控制递减、出范围/出售失效、光环无循环增益 |
| 冷却与时钟 | 暂停、1x/2x/3x、不同帧切分、升级/重建不刷技能，取消施放无误扣 |
| 地图 | 保留全部 57 项基线测试的覆盖语义，桥梁/禁建/两路/召唤继承不退化 |
| 存档与释放 | v1 有效存档迁移不丢数据，损坏回退，重复迁移；退出/重开/升级/出售没有持续资源增长 |

允许因契约升级调整旧断言，但必须解释行为变化并保留同等或更强覆盖，不能删掉失败用例掩盖回归。`sim.mjs` 当前即使结果是 LOSS/TIMEOUT 也返回退出码 0；读取退出码不等于通关或测试通过。

### 12.2 多策略定标

- 50 关全部输出结果，不只测草原第一关。建议至少四种策略：均衡防线、范围控制、持续伤害+支援、精英/飞行克制。未解锁阶段按该策略的合法可用塔回退。
- 机器人应懂两路覆盖、当前价格、技能时机和专精，不能继续只沿主路固定放八座旧塔后用其失败证明关卡不可玩。
- 每关至少有一个遵守经济/解锁/建造限制的通关记录，可以是合法脚本或可复现的玩家操作记录；不能通过额外发钱或跳过怪物生成达标。
- 每世界至少选起步、多路、Boss 三种代表关验证两种不同阵容有意义；其余策略不要求全胜，不以单一机器人胜率决定难度。
- 波级报告包含：金币各来源、投资时间、剩余金币、漏怪来源/路线、塔等级与分支、实际 HP/护盾伤害、控制时长、支援增益和空转比例。
- 支援塔不能因没有直接伤害被判无用；记录其辅助贡献，避免与受益塔的实际伤害重复计入团队总伤害。
- 单塔横扫、无限控制、无限召唤掉金、所有后期关卡长期无可买升级，均为待修平衡问题。

建议的节奏试配区间：正常 1x 且不含暂停/菜单时，前期约 4 至 7 分钟，中期 7 至 10 分钟，终局 10 至 14 分钟。此为调优目标，不是已经测得的结果；若实际体验偏拖，应据报告调整，不能只拉长模拟超时。

### 12.3 浏览器与性能

- 延续 [map-browser-check.mjs](tools/map-browser-check.mjs)，并拟新增玩法检查：十一塔翻页、升级、两分支、手动/自动技能、技能取消、暂停恢复、金币显示、战后统计及旧存档入口。
- 固定检查 1440×900、720×900、390×844、844×390，并做同向缩窗和横竖屏切换；小地图、技能面板、塔坞和文本不得遮挡或溢出。
- 生成五种主题与各类技能的桌面/移动截图，检查画布非空、动画推进、关键资源加载、点击/触摸命中、运行时错误与资源 404。能读图时补视觉核对；不能读图时明确说明，不把像素统计描述成人工审美验收。
- 建议基准场景：60 塔、160 敌人、400 在途弹体；压力场景：120 塔、400 敌人、1000 弹体，另含有上限的持续区域和光环。使用固定种子，不通过删除敌人/跳过伤害来压测过关。
- G0 先记录参考 CPU/GPU、浏览器、分辨率、DPR 和画质。建议正常场景桌面目标 60 FPS、帧耗时 p95 不超过 25ms；手机目标 30 FPS、p95 不超过 45ms。纯逻辑固定步的 p95 目标不超过 5ms，并与 G0 环境对比。
- 上述是待验证目标，不是任何设备的性能承诺。无实机时记录未验证项；软件渲染无头浏览器不替代真实 GPU 性能报告。
- 重复建造/升级/出售和进出关卡至少 10 轮，报告弹体、事件监听、geometry、texture、材质/状态对象等数量，不能出现无界增长。

### 12.4 最终交付门槛

- [ ] R1 至 R8 都有对应实现位置和验证证据，不以“基础框架完成”代替完整内容
- [ ] 保留 42×28、50 关、双路与地形氛围；不是退回小地图换性能
- [ ] 十一种塔均可从建造一路操作到 Lv.8、分支、技能和出售
- [ ] 怪物等级、精英/抗性、波次预算与金币账本在实际战斗中生效
- [ ] 50 关合法通关记录和代表关多阵容记录齐全
- [ ] 默认自动技能可正常玩，手动操作有收益但不是唯一生路
- [ ] 桌面/手机操作、存档、资源和性能已验证，不能验证的项目明确保留
- [ ] 本地体验地址、测试命令、结果文件、已知问题与下一步已写回工程

## 13. 下一个模型从哪里开始

### 13.1 当前可直接使用的命令

下面均是已有命令。每条独立执行并读取实际输出；规划文档不意味着需要现在自动启动所有任务。

```powershell
git status --short
node -v
npm.cmd test
Invoke-RestMethod http://127.0.0.1:8137/healthz
node tools/serve.mjs
node tools/sim.mjs 0 0
node tools/balance-probe.mjs 0 3
node tools/econprobe.mjs --smart --rows
node tools/map-browser-check.mjs
git diff --check
```

先检查服务健康，已有正确工程服务时不要重复启动；没有服务才运行 `serve.mjs`。默认地址 `http://127.0.0.1:8137/`，端口占用时查看脚本的配置方式并另用端口，不停止无关服务。PowerShell 若拦截 `npm.ps1`，使用 `npm.cmd`。

浏览器脚本需 Playwright，安装方式见其头部；当前工程使用过 `logs/browser-tools` 的本地测试依赖。遵循当轮可用的浏览器工具和权限，不假定接手环境一定具备同样工具。

### 13.2 建议启动描述

```text
请先读 GAMEPLAY_OVERHAUL_PLAN.md 和 TASK_STATE.md 的当前目标部分。
已有 42×28 大地图、50 关与地形氛围已获用户认可，不能回退。
接下来实施大地图战斗改造：攻击、金币、塔成长、怪物等级、技能，
并新增 6 种塔至至少 11 种。方案已写好，但新玩法尚未实施。
先核对工作区，按 G0 → G5 分阶段推进；不要一次重写全部模块。
每阶段要有本地可玩版本、实际测试结果和交接记录。
具体数值可以根据证据调整，用户明确的功能范围不能缩水。
未收到发布指令时不自动部署，也不能把本地结果称为线上已更新。
```

### 13.3 每阶段必须写回的交接记录

把简要状态写到 `TASK_STATE.md` 的当前目标部分，阶段细节与验证摘要追加到本文末尾。原始结果可放 `logs/`，但该目录通常忽略版本控制，因此关键摘要必须保留在可交接文档中。必要时拟新增 `docs/GAMEPLAY_BALANCE_REPORT.md` 存完整定标报告。

```text
日期 / 执行者：
完成阶段与需求编号：
改动文件与关键行为：
采用的参数、与 v1.0 不同之处及理由：
实际运行的命令、退出状态和关键结果：
基线与本轮指标对比：
结果/截图路径；是否人工看过图：
未测内容、已知问题和阻塞原因：
当前服务地址；是否发布（默认未发布）：
下一步唯一优先任务：
```

不要把已写代码但未验证的阶段勾为完成；不要复制历史 PASS 作为当前测试结果；不要覆盖其他人的未提交改动。只有确实无法继续且需要用户决定时才停下来提出具体问题，其余按阶段继续。

## 14. 初始决策与变更记录

| 决策 | v1.0 建议 | 调整条件 |
|---|---|---|
| 内容数量 | 保留 5 塔，新增 6 塔，总数至少 11 | 可再增加，不可减少用户要求 |
| 塔成长 | 8 级，Lv.4 招牌、Lv.6 二选一、Lv.8 终阶 | 根据流程和经济验证调整，仍需有实质成长和技能升级 |
| 新塔名单 | flame / venom / ballista / flak / arcane / beacon | 可替换名称或单塔设计，但保持六种独立用途及空地/支援覆盖 |
| 金币 | 自动到账，无利息/经济塔；开局和造价是试配值 | 根据 50 关预算和合法起防成本定标，不能照面积同比例放大 |
| 技能 | 默认自动，终阶可手动；不引入施法货币 | 手动收益可调，但不能要求高频逐塔操作才能过关 |
| 怪物 | 等级 + rank + 有限词缀，建议 Lv.1 至 Lv.100 | 按压力曲线定标，不叠乘两套等级倍率 |
| 技术 | 延续原生 JS、Three.js、现有模型与地图体系 | 只有明确瓶颈和必要证据时才改变模块边界 |
| 发布 | 本地实施与验收 | 需要后续明确发布请求，不因完成 G5 自动发布 |

### 2026-09-09：规划交付

- 已核对五塔五级、11 普通敌人、5 Boss、50 关、6 至 16 波及当前经济/索敌实现。
- 已交付本方案，并在 README 和 TASK_STATE 设置当前任务入口。
- `npm.cmd test`：57/57 通过；规划轮未改变玩法、数值、资产或存档。
- G0 至 G5 均未实施；没有新增任何可用塔或技能，也没有发布线上。

### 2026-09-09：G0/G1 战斗基础实施

- 完成 G0 基线记录：旧五塔、50 关经济与漏怪结果写入 `logs/g0-economy.txt` 和 `logs/g0-balance.txt`；基准智能策略通关 17/50，世界 1/2 为 10/10，世界 3 为 4/10，世界 4/5 为 0/10。该结果是旧五塔基线，不是最终平衡结论。
- 完成 G1 战斗基础：新增 `js/game/spatial-index.js` 与 `js/game/combat.js`；Battle 接入出生、移动、死亡、召唤、离场的敌人空间索引；目标筛选统一支持空中/地面和预计出界时间排序；伤害统一返回 HP/护盾/过量/击杀结果；炮击预判沿所属 sampler 取未来点；发射瞬间重新校验目标。
- 统一塔注册表读取：Battle、Tower、HUD、实战自动策略和三个离线探针不再各自维护建造成本；未知塔返回 `invalid`，不能按零成本建造。旧五塔定义和攻击方式保持兼容。
- 新增 `tools/combat.test.mjs`，覆盖空间索引、目标类型、多路线威胁排序、物理/法术/真实伤害、护盾、零伤害、过量伤害、死亡幂等、发射时射程校验和成本注册表；`npm.cmd test` 实际结果为 67/67 通过，地图测试仍覆盖 50 关。
- 实际回归：`node tools/sim.mjs 0 0` 为 WIN、6/6 波、38 击杀、20 生命；`node tools/balance-probe.mjs 0 0` 为 1/1 通关、0 漏怪；`git diff --check` 与涉及 JS 语法检查通过。
- 未完成/未宣称：当前仍只有 5 种塔、5 级，无技能、专精、怪物独立等级/词缀；尚未进行 50 关新系统全量定标、浏览器人工体验或发布。下一优先级为 G2：先建立八级成长/专精/技能通用结构，并落地毒蚀塔与指挥塔的最小可玩闭环。

### 2026-09-11：G2 成长与技能闭环

- 七塔全部扩展至八级；新增毒蚀塔与指挥塔，分别在 W1-7、W2-1 解锁。箭塔、毒蚀塔、指挥塔完成招牌、专精、终阶、升级外观和操作闭环；其余四塔的完整技能留待 G4。
- 新增 `effects.js` 与 `skills.js`；独立毒层、抑疗、范围毒弹、有界毒云、最强光环与临时增益限幅、自动/手动技能和冷却统一进入实际战斗。出售、到期与退出会清理相关效果和实例资源。
- 战斗使用固定 1/60 秒步进；暂停冻结战斗。修复毒伤死亡后的漏怪/索引重复处理与最终波最后一命漏怪胜负冲突。
- HUD 按结构变化重建，冷却和金币仅更新现有控件；七塔横竖屏布局、显式专精费用、终阶自动复选框、Lucide 图标与技能音效可用。技能 Z/X/F 不占用 Q/E 镜头操作。
- 实际验证：`npm.cmd test` 100/100；地图与新成长流程均通过 1440×900、720×900、390×844、844×390 检查；毒云显隐产生 5276 至 27224 个变化像素，不是空场景占位。截图与数值检查不冒充人工视觉评审或实机性能承诺。
- 默认固定八塔模拟：19/50 通关，1 关触及 600 秒预算；该关零基 2,4 在 900 秒预算内正常通关。新增阵容 `--g2 --branch=B` 为 16/50，0 超时。保留失败结果，交给 G3/G5 处理经济与长路线节奏，不把机器人胜率当作最终平衡结论。
- 完整证据、命令和限制：[docs/G2_VERIFICATION.md](docs/G2_VERIFICATION.md)。下一阶段直接进入 G3：怪物等级/rank/词缀、经济预算/收支账本、召唤家族奖励和 50 关统计。没有实施 G3 至 G5，没有发布线上。
